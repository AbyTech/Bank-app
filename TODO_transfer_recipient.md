# Transfer Recipient Name Display Update

## Tasks
- [ ] Modify TransferForm.jsx to display recipient name in a read-only input field instead of plain text

## Details
- Change the recipient name display from `<div>Account Holder: <strong>{recipientName}</strong></div>` to a disabled input field
- Ensure the input is styled consistently with other form inputs
- Test that the field populates when a valid account number is entered and clears for invalid ones
