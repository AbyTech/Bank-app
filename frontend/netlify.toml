[build]
  base = "frontend"
  publish = "dist"
  command = "npm install && npx vite build"

[build.environment]
  NODE_VERSION = "18"
  VITE_API_BASE_URL = "https://banking-back-pqlr.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
