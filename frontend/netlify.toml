[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "dist"


[build.environment]
  NODE_VERSION = "18"
  VITE_API_BASE_URL = "https://banking-back-pqlr.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
